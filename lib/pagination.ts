/** Compact lists on the pharmacy dashboard (low stock, expiring soon). */
export const DASHBOARD_PAGE_SIZE = 5;

/** Default page size for paginated tables across the app. */
export const DEFAULT_PAGE_SIZE = 10;

/** Page size when fetching all rows for PDF export (loops until done). */
export const PDF_EXPORT_PAGE_SIZE = 100;
