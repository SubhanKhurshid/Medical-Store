/**
 * Sales history page (tabs + sales graph / tables) — disabled.
 * Not imported anywhere active; route `/pharmacist/sales-history` returns null.
 * Restore implementation from version control if this feature is needed again.
 */
export default function SalesHistory() {
  return null;
}

/* ——— previous implementation (sales graph + period tables) was here ——— */
